pipeline {
   agent any

   stages {

      stage('build') {
        steps{
		  sh 'pip install pytest'
          sh 'pip install pyodbc'
		  sh 'pip install pymssql'
          sh 'pip install pytest-html'
        }
      }
   }
}
